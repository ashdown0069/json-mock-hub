/**
 * MCP 도구 이름 목록 (설명·예시는 messages의 WorkspaceMcp.tools.<name>에서 i18n).
 * 목록 자체는 @workspace/types가 단독 소유한다 — apps/mcp의 실제 등록과
 * 이 화면이 어긋나지 않도록 양쪽이 같은 배열을 쓴다.
 */
export { MCP_TOOL_NAMES, type McpToolName } from "@workspace/types"
